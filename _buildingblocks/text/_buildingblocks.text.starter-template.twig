{% spaceless %}

{# Variables #}
{% set moduleClass    = 'starter-template' %}
{% set content        = content is defined ? content : null %}

{# HTML #}
<div class="{{moduleClass}}">{{content}}</div>

{% endspaceless %}
